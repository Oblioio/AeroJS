(function(window) {

    'use strict';

    var Box = function ( width, height, depth, widthSegments, heightSegments, depthSegments ) {
        
        return;
    }

    // add section to Aero namespace
    Aero.Geometries = Aero.Geometries || {};
    Aero.Geometries.Box = Box;


}(window));
